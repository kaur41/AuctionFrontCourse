name: Simple Tests

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Check project structure
        run: |
          echo "=== Repository structure ==="
          ls -la
          echo ""
          echo "=== Project folder ==="
          ls -la ono-tebe-nado-ad-main/
          echo ""
          echo "=== HTML files ==="
          find . -name "*.html" -type f
          echo ""
          echo "=== Package.json ==="
          find . -name "package.json" -type f -exec cat {} \;
      
      - name: Run simple tests
        run: |
          echo "✅ Basic CI check passed"
          echo "If you see this, CI is working!"
